{
  "name": "mabl-verification-plugin",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Dev-only tooling for the mabl-verification AIDLC plugin. The published artifact is the packaged projection, not this package.",
  "scripts": {
    "sync": "bun scripts/sync-aidlc.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun scripts/build-projections.ts",
    "check": "bun run typecheck && bun test && bun scripts/build-projections.ts --check"
  },
  "devDependencies": {
    "bun-types": "^1.3.13",
    "typescript": "^5.6.0"
  }
}
